version: '3.8'

services:
  johnnymonkey:
    # Kein `image:`-Tag → Portainer/Docker versucht keinen Hub-Pull.
    # Image wird nur lokal aus dem Dockerfile gebaut.
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: build
    container_name: johnnymonkey-app
    # Host-Netz: App bindet direkt auf Host-Port 80 (umgeht Docker-Port-Proxy).
    # webserver/nginx auf Port 80 muss gestoppt sein.
    network_mode: host
    environment:
      - NODE_ENV=production
      - PORT=80
      - DATABASE_URL=file:./prisma/dev.db
      - LOCAL_MATERIALS_PATH=/app
      - JM_REIHEN_PATH=/app/J-M-Reihen
    volumes:
      # Persistente Datenbank (wird beim Start aus backup_latest.db synchronisiert)
      - johnnymonkey_db:/app/server/prisma
      # Material-Dateien
      - ./material:/app/material:ro
      # J-M-Reihen kommt aus dem Docker-Image
      # Optional: Logs für einfacheren Zugriff
      - johnnymonkey_logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      # Portainer Labels für bessere Organisation
      - "com.portainer.io.name=JohnnyMonkey"
      - "com.portainer.io.description=Educational Learning Platform"
      - "com.portainer.io.category=Application"
      - "com.portainer.io.managed=true"

  # Nginx nicht mehr benötigt - Sophos Firewall übernimmt HTTPS-Terminierung
  # Node.js Server läuft direkt auf Port 80 (host network)

volumes:
  postgres_data:
    name: johnnymonkey_postgres_data
  johnnymonkey_db:
    name: johnnymonkey_database
    driver: local
  johnnymonkey_logs:
    name: johnnymonkey_logs
    driver: local
